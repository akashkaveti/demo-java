name: "Code Scanning"
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
  # schedule:
  #   - cron: '0 18 * * 6'permissions:
  actions: write
  contents: read
  security-events: write
  packages: read
  pull-requests: read
  jobs:
  code-scan-analysis:
    name: Code Scan Analysis
    uses: meltwater/sec-global-gh-actions/.github/workflows/codeql-horusec-analysis.yml@NIGHT-668
    secrets: inherit